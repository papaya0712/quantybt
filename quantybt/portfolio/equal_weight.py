import pandas as pd
import numpy as np
import plotly.graph_objects as go
import plotly.express as px
from plotly.subplots import make_subplots

from quantybt.portfolio.statistics import max_drawdown, sharpe, VaR, CVaR, EVaR
from quantybt.portfolio.base import BaseModel

class EqualWeightPortfolio(BaseModel):
       """Simulates a 1/n weighted portfolio and calculates new portfolio metrics"""
       def __init__(self):
              return
       
       def run(self):
              return
       
       def plot(self):
              return
       
#